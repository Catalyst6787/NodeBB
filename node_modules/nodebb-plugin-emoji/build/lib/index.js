"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
var _a, _b, _c;
Object.defineProperty(exports, "__esModule", { value: true });
exports.parse = void 0;
exports.init = init;
exports.adminMenu = adminMenu;
exports.composerFormatting = composerFormatting;
exports.addStylesheet = addStylesheet;
exports.filterMessagingLoadRoom = filterMessagingLoadRoom;
exports.configGet = configGet;
const settings = __importStar(require("./settings"));
const parse = __importStar(require("./parse"));
exports.parse = parse;
const build_1 = require("./build");
const pubsub_1 = require("./pubsub");
const controllers_1 = __importDefault(require("./controllers"));
const base_url_1 = require("./base-url");
const nconf = (_a = require.main) === null || _a === void 0 ? void 0 : _a.require('nconf');
const buster = (_b = require.main) === null || _b === void 0 ? void 0 : _b.require('./src/meta').config['cache-buster'];
const file = (_c = require.main) === null || _c === void 0 ? void 0 : _c.require('./src/file');
async function init(params) {
    (0, controllers_1.default)(params);
    const { parseAscii, parseNative, parseTitles } = await settings.get();
    const baseUrl = (0, base_url_1.getBaseUrl)();
    // initialize parser flags
    parse.setOptions({
        ascii: parseAscii,
        native: parseNative,
        titles: parseTitles,
        baseUrl,
    });
    // always build on startup if in dev mode
    const shouldBuild = nconf.any('build_emoji', 'BUILD_EMOJI') ||
        // otherwise, build if never built before
        !(await file.exists(build_1.tableFile));
    if (shouldBuild) {
        await (0, pubsub_1.build)();
    }
}
async function adminMenu(header) {
    header.plugins.push({
        route: '/plugins/emoji',
        icon: 'fa-smile-o',
        name: 'Emoji',
    });
    return header;
}
async function composerFormatting(data) {
    data.options.push({
        name: 'emoji-add-emoji',
        className: 'fa fa-smile-o emoji-add-emoji',
        title: '[[emoji:composer.title]]',
    });
    return data;
}
async function addStylesheet(data) {
    const baseUrl = (0, base_url_1.getBaseUrl)();
    data.links.push({
        rel: 'stylesheet',
        href: `${baseUrl}/plugins/nodebb-plugin-emoji/emoji/styles.css?${buster}`,
    });
    return data;
}
async function filterMessagingLoadRoom(data) {
    if (data && data.room && Array.isArray(data.room.composerActions)) {
        data.room.composerActions.push({
            action: 'emoji',
            class: 'd-none d-md-flex',
            icon: 'fa-smile',
            title: '[[emoji:composer.title]]',
        });
    }
    return data;
}
async function configGet(config) {
    const customFirst = await settings.getOne('customFirst');
    // eslint-disable-next-line no-param-reassign
    config.emojiCustomFirst = customFirst;
    return config;
}
//# sourceMappingURL=index.js.map